#include "knapsack.h"

namespace knapsack {

auto maximum_value(int max_weight, std::vector<Item> const& items)
    -> std::vector<Item> {
    auto result = std::vector<Item>{};

    // (11, 3), (1,1), (1,1)

    return result;
}

} // namespace knapsack
